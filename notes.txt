To enable JIT trace

	-XX:+PrintCompilation -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining

Should Reflector.map(...) throw exception if key (field) not found? Use Strategy?

TODO

Code generate POJO's JSONWriter.

Utility from JSON to POJO.

Use NullObjects to skip unknown fields during recursion.

Throw ClassNotFoundException when JavaCompiler not found. Right?

Square MemoryBased's ClassLoader with ReflectASM's, for security, chaining, etc.

UnitTests for ParseExceptions.

Do something about Date.
Do something about Enum.

